apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-images
  labels:
    grafana_dashboard: "1"
data:
{{- range $path, $file := .Files.Glob "dashboards/*.json" }}
  {{ base $path }}: |-
{{ $file | toString | indent 4 }}
{{- end }}
